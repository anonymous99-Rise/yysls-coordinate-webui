{
  "name": "yysls-coordinate-webui",
  "version": "0.2.0",
  "private": true,
  "description": "燕云十六声坐标库 WebUI —— 搜索 / 筛选 / 地图散点 / 附近查询 / 批量导出",
  "type": "module",
  "scripts": {
    "sync": "node ../tools/sync_web_data.mjs",
    "predev": "npm run sync",
    "dev": "vite",
    "prebuild": "npm run sync",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node ../tools/test_core.mjs"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
