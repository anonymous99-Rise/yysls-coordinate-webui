# 更新日志

本文件由 `tools/bump_version.py` 维护。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## v0.3.0 — 2026-09-22

在真实运行的《燕云十六声》（v1.9.69）上把工具链跑通，并据此补上两块缺失能力。

### 新增

**OCR 读屏**

- `rpa/ocr.py` —— RapidOCR（PP-OCRv4）读屏文字，带不依赖游戏的自检。
  选它的依据：同机运行的「律匠」用的就是它，说明**在这个游戏的实际画面上验证过可用**。
- `rpa/find_coords.py` —— 多尺度 + 分区 OCR 扫描，专门把界面上的数字对找出来。

**实测脚本**

- `rpa/probe_window.py`、`rpa/probe_world_input.py` —— 游戏内抓屏与发键实测。
- `tools/probe_subregion.py` —— 官方坐标系性质的判据（见下）。

**窗口聚焦（`injector.py`）**

- `focus_window()` / `ensure_foreground()` —— 三级兜底把目标窗口调到前台。

### 实测结论

- 抓屏 42 ms/帧；自研 NCC 与 `cv2.matchTemplate` 结果一致（score=1.00000）。
- **世界模式下发键有效**：按 `m` 帧差 67.62 —— DirectInput 扫描码链路可用。
  判据是画面帧差，不是「函数返回 True」（后者只说明 `SendInput` 调用成功）。
- **游戏 UI 任何地方都不显示世界坐标**：世界 HUD、大地图界面、地图上悬停 6 处、
  地图右键菜单，全部实测确认。原始数据 4 位小数的精度必然来自程序化来源，
  因此「进游戏抄界碑坐标补 map2~8 锚点」这条路不可行 —— 文档第 9 节已给出替代方案。

### 变更

- `requirements-dev.txt` 加 `rapidocr-onnxruntime`（可选）。装它会顺带装上
  `opencv-python`，而 cv2 可用后模板匹配**快 6 倍**（20.4 ms vs 127.2 ms），
  并且自检里那条一直「跳过」的**对照实现**才会真正跑起来 ——
  两套完全独立的实现给出同一位置，是匹配正确性最有力的证据。

### 修复

- **`find_window` 被同名第三方工具骗到**：律匠的窗口标题是
  「律匠 - 燕云十六声装备调律工具 v0.12.2」，**同样含「燕云十六声」**，
  于是 `find_window("燕云十六声")` 返回了律匠的窗口，后续操作全打错对象 ——
  它不报错、只是没反应，极难排查。改为多命中时优先完全相等、其次标题更短的，
  并新增 `find_game_window()` 用**进程名 + 标题**双重限定。
- **缺聚焦能力**：Windows 前台锁会让 `SetForegroundWindow` **静默失败**
  （返回 0、不抛异常），表现为「脚本以为切过去了，键全打到别的窗口上」。
  同时补全 `GetForegroundWindow` / `SetForegroundWindow` / `AttachThreadInput`
  等的 `argtypes`/`restype` —— ctypes 默认返回 `c_int` 会把 64 位 HWND 截断。

### 文档

- 《官方数据接入设计》新增第 8、9 节：游戏内实测记录，以及修正后的架构 ——
  **以官方坐标系为准**（官方数据原样存放，手工数据换算进 map0/map1 官方坐标系），
  地图显示与检索不再需要进游戏，blocking 依赖直接消失。

## v0.2.0 — 2026-09-21

### 新增

**官方大地图数据接入**

- 接入官方大地图 8192 个点位（9 张图 / 24 个分类）。点位总数 **6071 → 9318**；
  传送点 18 → 289（含已有手工点），宝箱 1284 → 4334。
- **解出「官方画布经纬度 → 游戏世界坐标」的换算关系**，逐图独立，
  交叉验证相对误差 **清河 2.35% / 开封 2.14%**。
- 官方 `description` 字段是解谜与获取方法（「用太极把旁边的熊扔到石堆上，里面有宝箱」），
  已随点位进入 `note` 字段，ETL 未改动。

**新增工具**

- `tools/fetch_official.py` —— 拉取官方点位并本地缓存，支持离线重跑。
- `tools/fit_official_transform.py` —— 逐图解算换算参数，带 LOO 交叉验证、
  模型对比与外点剔除，并明确报告哪些图还缺锚点。
- `tools/import_official.py` —— 换算并写入 `raw/official/<图>/<分类>.ini`，
  与手工数据同一套流水线。
- `data/anchors_manual.json` —— 游戏内实测锚点入口，用于解开手工库未覆盖的 7 张图。
- `docs/官方数据接入设计.md` —— 完整推导、踩坑记录与游戏内锚点采集协议。

### 变更

- `raw/official/` 加入 `00_import_raw.py --force` 的保留名单（与 `raw/rpa/` 同级），
  重跑导入不会误删官方数据。
- `taxonomy.PATH_CATEGORY` 补上官方 24 个分类到本项目 12 类的映射。

### 修复

- **模型族选窄导致换算不可用**：官方画布相对游戏世界是**镜像**的（仿射线性部分行列式为负），
  而只允许旋转的相似变换永远拟不出镜像，误差高达 25%~42%。
  改用镜像相似变换后降到 2.35% / 2.14%，且比 6 参数仿射更准、能外推。
- **锚点重复喂入**：同一个官方点同时对上我方「地点标记」与「界碑」（相距可达 137），
  几何自相矛盾，把相对误差从 2.2% 拖到 24.5%。改为一个官方点对恰好一个我方点。
- **RANSAC 只收缩不增长**：`best` 初始化为全集使更新条件永不成立，
  函数退化为收缩到自洽小子集，在 5 点上报出虚低的 0.92%（选择偏差）。
  修正并把默认改为不剔除 —— 全集最大残差仅跨度的 2.2%，本无错配点。
- **编号命名规则过宽**：`len(ps) > 50` 误把「见闻」（518/521 名字唯一）编成序号名，
  丢失官方最有信息量的一批名字。改为只看名字重复率。

## v0.1.0 — 2026-09-21

首次公开发布。

### 新增

**数据层**
- 把 122 个手工坐标文件（GBK/UTF-8 混用、`.ini` 实为扁平 CSV、78% 重复行）整编为 **6071 个规范点位**。
- 27,099 行原始坐标零丢失、可追溯（每个点带来源文件 + 行号）。
- 分类与区域判定全程记录依据（`categorySource` / `regionSource`），可回溯、可复核。
- 补齐 24 个官方区域名（来自官方大地图接口），地点覆盖率 368 → 454。

**WebUI**（Vite + React + TS，零后端）
- 列表 / 地图双视图；7 组筛选；字段前缀搜索（`cat:` `region:` `mat:` `flag:` `src:`）。
- Canvas 散点地图：缩放平移、四种着色、图例开关、坐标刻度。
- 附近查询（贴入当前坐标找最近的点）、批量导出、收集进度、作者说明、数据体检。
- 全部筛选状态写进 URL hash，可分享。

**自动采集**（纯 Python，四个模块）
- `injector.py` 动手：DirectInput 扫描码发键、鼠标点击（前台 / 后台）、全局热键、拟人化抖动。
- `vision.py` 看见：GDI `CreateDIBSection` 截图、FFT 归一化互相关模板匹配、帧差到达检测。
- `mapnav.py` 认路：世界坐标 → 地图像素（相似 / 仿射拟合，Y 翻转自动诊断）。
- `navigate.py` 移动：调坐骑「识途」自动寻路，帧差判断到没到。
- `gather.py` 采集：连点器 / 有判断 / 定点确认三种模式。

**影刀 RPA 资产**（保留，但不推荐用于本场景，原因见 README）
- 影刀「执行Python代码」指令片段、任务清单生成器、指令级搭建指南。

### 修复

整编过程中发现并处理的数据损伤：
- 3 处两行粘成一行（缺换行）→ 自动拆开
- 1 处手滑多余前缀 `3-` → 剥掉
- 1 条只有 x,y,z 没有名称 → 标记并在导出时补占位名
- 412 条占位名（「示例」「未备注地点」）→ 改按来源文件判定分类

开发过程中自查出的自身缺陷（都加了回归测试）：
- `--duration` 一名两用，导致 `--duration 2` 把 F 键按住 2 秒、循环只跑 1 轮
- 纯色模板时 NCC 分母为 0，静默返回 0 分，看起来像「在左上角匹配到 0」
- 自检里的「cv2 对照」在 cv2 未安装时其实跑的是自研实现 —— 测试在撒谎
- `mapnav.Transform` 字段顺序错位，打印的「最大误差」一直是点的个数
- 锚点分布质量用条件数评估，被世界坐标绝对值主导，铺得最开的样本也被判「分布差」
- `navigate.py` 的 argparse help 嵌套双引号，语法错误

### 已知局限

- **游戏内标定尚未完成**：`mapnav.py` 的变换数学与整条链路都通过了合成验证，
  但地图像素需要你开着游戏量 2~4 个界碑图标才能标定。在标定完成前，
  `navigate.py` 只能 dry-run。
- 分类仍剩 15 个点位未归类（均为任务步骤描述）。
- 官方地图另外 7 张图（河西 / 青州 / 江南 / 不见山 / 滹沱 / 开封皇宫 / 风催凉州）我们没有点位。
- 曾尝试用同名地点直接解出两坐标系关系，**结论是不成立**（最大一致子集仅 4/20，
  因为官方给的是区域标签锚点，不是具体点位）。详见 README。

### 说明

原始坐标由 **Hermit**、**小狼** 等人无偿分享，本仓库只做格式整编。
沿用原作者的要求：**免费分享请勿倒卖，转发请标注原作者。**
